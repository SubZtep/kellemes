name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  root-release:
    name: Release root
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.23.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24.11.1
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Semantic release (root)
        uses: cycjimmy/semantic-release-action@v6
        with:
          working_directory: .
          extra_plugins: |
            @semantic-release/changelog
            @semantic-release/git
            semantic-release-monorepo
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  release-common:
    name: Release @kellemes/common
    runs-on: ubuntu-latest
    needs: root-release
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.23.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24.11.1
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Semantic release (common)
        uses: cycjimmy/semantic-release-action@v6
        with:
          working_directory: packages/common
          extra_plugins: |
            @semantic-release/changelog
            @semantic-release/git
            semantic-release-monorepo
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  release-vector-service:
    name: Release @kellemes/vector-service
    runs-on: ubuntu-latest
    needs: root-release
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.23.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24.11.1
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Semantic release (vector-service)
        uses: cycjimmy/semantic-release-action@v6
        with:
          working_directory: packages/vector-service
          extra_plugins: |
            @semantic-release/changelog
            @semantic-release/git
            semantic-release-monorepo
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  release-rag:
    name: Release @kellemes/rag
    runs-on: ubuntu-latest
    needs: root-release
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.23.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24.11.1
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Semantic release (rag)
        uses: cycjimmy/semantic-release-action@v6
        with:
          working_directory: packages/rag
          extra_plugins: |
            @semantic-release/changelog
            @semantic-release/git
            semantic-release-monorepo
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  release-api:
    name: Release @kellemes/api
    runs-on: ubuntu-latest
    needs: root-release
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.23.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24.11.1
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Semantic release (api)
        uses: cycjimmy/semantic-release-action@v6
        with:
          working_directory: packages/api
          extra_plugins: |
            @semantic-release/changelog
            @semantic-release/git
            semantic-release-monorepo
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  release-cli:
    name: Release @kellemes/cli
    runs-on: ubuntu-latest
    needs: root-release
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.23.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24.11.1
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Semantic release (cli)
        uses: cycjimmy/semantic-release-action@v6
        with:
          working_directory: packages/cli
          extra_plugins: |
            @semantic-release/changelog
            @semantic-release/git
            semantic-release-monorepo
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
